class Solution:
    def returnToBoundaryCount(self, nums: List[int]) -> int:
        a=0
        x=0
        for i in range(len(nums)):
            a+=nums[i]
            if a==0:
                x+=1
        return x
